'use client';

import { styled } from 'styled-components';

export const MagneticItem = styled.span`
  position: relative;
  z-index: 1;
  display: inline-flex;
  align-items: center;
  gap: 0.6rem;
  width: max-content;
  max-width: none;
  word-break: normal;
  white-space: nowrap;
`;
